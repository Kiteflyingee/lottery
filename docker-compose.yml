services:
  lottery-app:
    build: .
    ports:
      - "3000:3000"   # 统一入口，Nginx 反向代理
    volumes:
      - lottery-data:/app/server  # 持久化数据库
    restart: unless-stopped
    environment:
      - NODE_ENV=production

volumes:
  lottery-data:
